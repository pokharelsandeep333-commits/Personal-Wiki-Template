# Lint Checklist

Before committing changes, ensure the following checks pass:
- [ ] All notes have valid YAML frontmatter.
- [ ] All notes under `Wiki/` contain links to at least one source in `Raw/Sources/`.
- [ ] No orphaned links exist.
- [ ] File names follow the naming conventions.
- [ ] Claims in `Wiki/` are supported by the linked raw sources.
